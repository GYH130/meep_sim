# 2026-09-24 收尾与下次交接

## 当前结论

L 于北京时间 18:18 正常结束队列；18:32 核查无在运行求解进程、无未关闭预算区间。三例均保留 `NUMERICAL_FAILED`，不是崩溃或超时。未启动完整 50 μm 验证、斜槽或 20 条件尺寸试算。本次收尾不追加计算、不关闭服务器、不修改旧结果。

指定 Ordal Route A Ti 模型，10.5 μm、p 偏振、观察角 +30°、r32/Courant0.25；均匀平面计算宽度 2 μm，仅是边界诊断，不是加工周期改变。Ti 厚度 60 μm，无氧化层。材料与求解核心未改动。

| Absorber 参数 | 空背景伪反射 | 平面 R | 与解析 R 绝对差 | 独立吸收差 | 结果 |
|---|---:|---:|---:|---:|---|
| 1e-15 对照 | 0.001716655 | 未运行 | — | — | 成功复现 K 失败，仍不合格 |
| 1e-8 | 0.000507729 | 0.926206988 | 0.029985928 | 0.030561063 | 平面不合格 |
| 1e-6 | 0.000226348 | 0.934086325 | 0.022106591 | 0.023352478 | 平面不合格 |

门槛：空背景 <0.001；R 解析误差 <0.0025；独立吸收 `|A_vol-A_flux|<0.01`。筛选还要求空背景 <0.0005，1e-6 满足这一额外要求，但不能抵消平面失败。两候选的分阶闭合通过。三例停止条件通过，两平面源后停止时刻为 586.0078125，满足三个窗口要求；不能以延长时间为默认修复。

解析 R=0.956192916、A=0.043807084。两候选 A_vol 分别为 0.043231948、0.042561203，距离解析吸收约 0.000575、0.001246，明显小于通量吸收的误差。这是优先检查入射归一化、复场背景扣除、边界返回波和源的相互作用的证据，**不是已经确定根因，也不能据此单独认证 A_vol**。

窄域对照与旧 K 伪反射相差约 5e-17，单位宽度入射功率也一致，证明本次均匀参考的缩域复现有效；不能推广为斜槽掠射衍射阶或全宽计算已合格。历史 H 平面仍为已通过证据：R≈0.957002935，独立吸收差≈3.39e-7；H 斜槽尾场仍未通过。不得写成“以前所有计算都失败”或“L 已经修好斜槽”。

收尾时新增一个**已重算、未认证**的离线线索：用保存的平面总复场 `total_Ex/total_Hz`，按本例空气中 p 波 `H_up=(Hz-Ex/cos30°)/2`、`H_down=(Hz+Ex/cos30°)/2`，计算同平面加权功率比，两候选分别为 **0.957002904866（1e-8）**、**0.957002902144（1e-6）**；与解析差约0.000810，彼此仅差2.7e-9。与正式 R 的明显差异使“非零参考反向波、相干背景扣除、实际入射功率归一化”成为首要排查路径。仍须检查Yee配准、阻抗近似和独立能量账，不更改任何正式状态。同目录 `replay_plane_ratio.py` 可读取上述原始包重现，只依赖NumPy、不调用Meep、不写文件；命令为 `python -B replay_plane_ratio.py /path/to/L_raw_evidence_20260924T101823822823Z.tar.gz`。

## 成本与保存

- L 实际求解区间并集 **770.908 秒（12.85 分钟）**，4 MPI rank，**0.85656 rank-hours**；三例未重试。
- 共享账本累计 **29.40393 小时**、117.61572 rank-hours、20 次任务记录；48 小时额度理论余 18.59607 小时，但租期是另一独立约束，不授权使用剩余额度。队列不调用 AI；本次开发/分析实际 token 与人民币账单不可得，不虚报为零。
- 服务器阶段根目录：`/root/autodl-tmp/meep_sim/meep_screen/boundary_screen_L/`。结果：`runs/boundary_20260924_L/tasks/*/attempt_1/result.json`；图：`runs/boundary_20260924_L/reports/boundary_screen.png`。
- 本机原始证据：`meep_screen/backups/20260922_expiry/L_boundary_screen/L_raw_evidence_20260924T101823822823Z.tar.gz`（约32.2 MB）。SHA256：`0e96c13a36eb92e8b5fd9d01117c3a247f4493cc38a771d6ce3427fc2c6a3096`。本轮数据快照已在提交 `5138fbbea1fb59444fd9587aa90dbc4440f1fc2f` 保存；本交接说明另追加归档。
- GitHub 仅归档分支 `codex/archive-20260922-ti2d`，目录 `meep_screen_archive/boundary_screen_L/`。大型场数组只在本机原始包，不在 GitHub。不要只下载 GitHub 后以为拥有全部原始场证据。
- H/I/J/K/L 原始包本轮全部重新核对 SHA256。L 原始包内部127个文件和精简快照92个文件指纹一致；5个受保护的旧改动文件未变化。详细凭证见同目录 `ARCHIVE_VERIFICATION_20260924.json`。本机数据建议另做一份独立备份，不能依赖即将到期的服务器。

## 下次待解决的问题（按顺序）

1. **为什么空背景合格，正式 R/A_flux 仍偏离解析，而 A_vol 和同平面总场比接近解析？** 先重现本交接的离线线索，再利用已保存的 `reference_planes.npz`、`monitor_planes.npz` 和 `volume_ED.npz`，独立核算入射/反射复振幅、相位、前后向功率、归一化与背景扣除。只列证据和反例，不先决定是边界、源还是积分代码有错；不能乘经验系数或裁剪强行闭合。
2. **Absorber 强度改变的是测量误差，还是源/边界返回波共同改变了实际入射场？** 与通过的 H 平面证据比较配置及相同量。设计一个能区分假设的最小均匀域对照；只有离线证据不足、服务器和预算确认后才运行，不穷举参数。
3. **怎样处理真实斜槽尾场/掠射阶？** 窄域平面不含真实周期全部衍射阶，不能直接套用于斜槽。先恢复平面验收，再全50 μm验证，之后只推进一次基准斜槽。两轮针对性修复仍失败即停，不再堆叠补丁。
4. **何时进入加工参数试算？** 仍需网格、严格停止、镜像/直槽、s偏振、L48边界几何等原计划门槛。通过后才按锁定20条件输出局部差分敏感性，不声称峰角、最优尺寸或三维样品规律。

## 可交给下次 AI 的起始 Prompt

继续纯Ti二维斜槽项目，先只读本交接说明、L screen_summary、两候选 result 的 checks/metrics/analytical 及 H_flat32p_result 的对应字段。原始数组在本机 L/H 归档，按需提取并校验，禁止从缺失数组捏造结果。先做无需FDTD的功率归一化/背景复场审计，回答上面问题1、2，给出一个能区分原因的最小对照及资源估计；未经确认新服务器与预算，不启动计算。保留原标准、旧证据和失败标签，不重跑D00/D04，不重新拟合材料，不靠改阈值、裁剪或经验系数通过。修复必须配独立测试。后续通过完整门槛才进行20条件尺寸试算。本机不运行FDTD，所有求解留在服务器数据盘。每轮只读短摘要，后台队列独立运行，不让大模型轮询等待。此次租期和20:30截止仅适用于2026-09-24，不能当作未来授权。
