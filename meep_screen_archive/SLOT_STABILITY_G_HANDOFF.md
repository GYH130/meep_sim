# G：补做未运行的关闭平滑组

## 范围

用户授权“开启下一步”：修复停止证据校验、离线复核已有开启平滑结果，仅执行尚未运行的关闭平滑组。不重新计算开启组，不改材料、网格、时间步、槽形、增长阈值或生产求解器，不进入尺寸扫描。

F 原目录及原账本历史保持原样。F 的监管状态 CRASHED 不回写为成功；G 的 `offline_reaudit.json` 单独说明：探针以退出码 0 停止，真实触发窗口为 t=16/18/20，而停止后多保存了 t=20.0078125 的快照。修复后的校验定位首次触发窗口，不以末三点替代该窗口。额外快照、原始场和哈希证据全部保留。

## 必须守住的条件

- 修复后必须通过真实末点序列的回归测试，以及伪造触发、非固定采样间隔、证据缺失等拒绝测试。
- 两组仍使用同一冻结探针和 Ti 模型；唯一物理差别是 `eps_averaging`。原生产 `ti2d` 和 Meep 安装不变。
- 新队列只允许运行 `averaging_off` 一次，从 t=0 开始；不宣称接续旧时间步。
- 保留 F 原累计起点 30952.50267100334 秒，F 已花费 241.523697376 秒；F+G 共享 1800 秒诊断上限，不重新给 30 分钟。
- 单组含关停上限 1200 秒；累计求解仍受原 172800 秒（48 小时）限制。4 MPI ranks、每 rank 单线程；进程树内存不超过启动时容器可用内存的 70%。
- MPI 主进程及本次会话内所有计算进程确认退出后才闭账；失败不重试。
- 最大仿真时间 t=100，源约在 t=525 才结束。短测稳定不等于完整稳定，更不等于光学性能验证通过。

## 文件和操作

服务器阶段目录：`/root/autodl-tmp/meep_sim/meep_screen`。

- 原始开启组：`repair_diagnostics/slot_stability_20260922_F/averaging_on/`（只读复用诊断证据）。
- 新目录：`repair_diagnostics/slot_stability_20260922_G_off/`。
- `offline_reaudit.json`：第一次诊断的独立复核及触发证据。
- `plan.json`、`authorization.json`：原始来源哈希、修复实现哈希、一次性执行清单及预算历史。
- `status.json`：当前短状态；结束后的 `report.md`、`report.json` 联合两组诊断结果。
- `averaging_off/trace.json`、小区域复场 NPZ、`process.json`：关闭组新增证据与资源。

入口：`python -B run_slot_stability_off.py --prepare`（无 FDTD），随后由本次会话一次性后台启动 `--run`。不要手工重复启动；准备与运行均拒绝覆盖或重试。

结束后无论结果如何，均保持 `qualification=false`。只有“开启组真实增长、关闭组完整到 t=100 且无增长”才支持界面平滑相关的早期稳定性差异；尚不能宣称完整修复。AI 在一次启动检查后结束，不持续轮询；下一次读取短报告再决定是否申请完整验证。
