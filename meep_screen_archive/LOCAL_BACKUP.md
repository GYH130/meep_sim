# 本轮备份与后台任务

- GitHub 新归档分支：<https://github.com/GYH130/meep_sim/tree/codex/archive-20260922-ti2d>。`main`、旧研究分支不变；归档分支是独立快照，不用于直接合并。
- 已保存本地原始包 `preexpiry_evidence_20260922.tar.gz`（约576MiB）。SHA256：`17b1bc4eae4388bdcd1ccfe15496097ce20305102d0d3e5de1ab4eb8b1a24573`。
- 服务器已启动 H 队列：最多10个验证条件，串行4 ranks、每例最多6小时、失败停扩展、保留原48小时累计预算。关闭平滑的完整稳定性尚未证明，不能据此宣布开始正式尺寸规律扫描。
- 求解硬截止：2026-09-23 **16:52:44北京时间**；按用户给出的剩余24小时预留4小时，租赁平台实际到期时间仍需用户确认。

`collect_once.py` 是本机一次性取回程序：等待服务器导出完成→SHA256及敏感信息检查→更新既有归档分支（非强推）→下载并校验新H原始证据包。无AI/API调用，不消耗模型token。最多等待至次日17:52:44，整体备份最晚次日20:22:44结束。故障写 `collector_status.json`，不伪报完成。

**请保持这台Mac唤醒、联网，勿合盖或退出后台进程。** 取回依赖本机现有SSH身份；GitHub或网络认证变化会使备份停止。服务器求解独立运行，即使Mac断线也不自动停止，但不等于数据已安全下载。

查看进展只需打开 `collector_status.json`。最终应为 `COMPLETE`，包含GitHub提交SHA和本地 `H_raw_evidence.tar.gz`校验值。不满足这两项前，不应主动释放服务器。若本机断线，可在截止前重新运行一次 `python3 collect_once.py`；已下载文件通过rsync续传，但这不是FDTD时间步恢复。

GitHub仅保存代码、材料、配置、精简证据/报告及大文件指纹。完整原始场留本地包，不把API密钥、SSH私钥、缓存、完整会话上传。单案例合格不等于全套物理资格或尺寸趋势已通过。
